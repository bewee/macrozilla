{
  "title": "eval",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "allOf": [{"$ref": "/class"}],
  "properties": {
    "qualifier": {
      "enum": ["not", "abs", "negate", "&", "|", "^", "+", "-", "*", "/", "pow", "%", "round", "=", ">", "<", ">=", "<=", "!="]
    },
    "operand": {"$ref": "/class"},
    "left": {"$ref": "/class"},
    "right": {"$ref": "/class"}
  },
  "required": ["qualifier"], 
  "oneOf": [
    {
      "properties": {
        "qualifier": {"enum": ["not", "abs", "negate"]}
      },
      "required": ["operand"]
    },
    {
      "properties": {
        "qualifier": {"enum": ["&", "|", "^", "+", "-", "*", "/", "pow", "%", "round", "=", ">", "<", ">=", "<=", "!="]}
      },
      "required": ["left", "right"]
    }
  ]
}