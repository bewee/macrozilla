{
  "title": "exec",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "allOf": [{"$ref": "/class"}],
  "properties": {
    "qualifier": {
      "enum": ["++","--","invert"]
    },
    "operand": {"$ref": "/class"}
  },
  "required": ["qualifier", "operand"]
}