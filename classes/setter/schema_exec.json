{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "allOf": [{"$ref": "/class"}],
  "properties": {
    "attribute": {"$ref": "/class"},
    "value": {"$ref": "/class"},
    "speed": {"$ref": "/class"},
    "time": {"$ref": "/class"},
    "ups": {"$ref": "/class"}
  },
  "required": ["attribute", "value"],
  "dependencies": {
    "ups": {
      "oneOf": [
        {
          "required": ["speed"]
        },
        {
          "required": ["time"]
        }
      ]
    },
    "speed": {
      "not": {"required": ["time"]}
    },
    "time": {
      "not": {"required": ["speed"]}
    }
  }
}