{
  "title": "exec",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "allOf": [{"$ref": "/class"}],
  "properties": {
    "qualifier": {"enum": ["set", "set_w_speed", "set_w_time"]},
    "attribute": {"$ref": "/class"},
    "value": {"$ref": "/class"},
    "speed": {"$ref": "/class"},
    "time": {"$ref": "/class"},
    "ups": {"type": "integer"}
  },
  "required": ["qualifier", "attribute", "value"],
  "dependencies": {
    "ups": {
      "properties": {
        "qualifier": {
          "enum": ["set_w_speed", "set_w_time"]
        }
      }
    }
  },
  "oneOf": [
    {
      "properties": {
        "qualifier": {
          "enum": ["set_w_speed"]
        }
      },
      "required": ["speed"]
    },
    {
      "properties": {
        "qualifier": {
          "enum": ["set_w_time"]
        }
      },
      "required": ["time"]
    }
  ]
}