{
  "title": "exec",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "allOf": [{"$ref": "/class"}],
  "properties": {
    "thing": {
      "type": "string"
    },
    "property": {
      "type": "string"
    },
    "action": {
      "type": "string"
    },
    "function": {
      "type": "string",
      "enum": ["next", "prev", "action"]
    }
  },
  "required": ["thing", "function"],
  "oneOf": [
    {
      "properties": {
        "function": {"enum": ["next", "prev"]}
      },
      "required": ["property"]
    },
    {
      "properties": {
        "function": {"enum": ["action"]}
      },
      "required": ["action"]
    }
  ]
}